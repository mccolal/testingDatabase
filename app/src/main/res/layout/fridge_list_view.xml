<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="6dip" >

    <ImageView
        android:id="@+id/icon"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="6dip"
        android:layout_marginEnd="6dip"
        android:src="@mipmap/ic_fridge" />

    <TextView
        android:id="@+id/fridgeDesc"
        android:layout_width="270dp"
        android:layout_height="26dip"
        android:layout_alignParentBottom="true"
        android:layout_toRightOf="@id/icon"
        android:ellipsize="marquee"
        android:maxLines="1"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/fridgeName"
        android:layout_width="270dip"
        android:layout_height="wrap_content"
        android:layout_above="@id/fridgeDesc"
        android:layout_alignParentTop="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toRightOf="@id/icon"
        android:gravity="center_vertical"
        android:textSize="16sp" />

    <Button
        android:id="@+id/removeFridge"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@mipmap/ic_remove"
        android:paddingLeft="15dp"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@+id/fridgeDesc"
        android:layout_toEndOf="@+id/fridgeDesc" />

</RelativeLayout>